# Prod overrides — used when AI_DIGEST_ENV=prod
environment: prod

llm:
  model: agent
  provider: agent

delivery:
  output_mode: telegram

observability:
  enabled: true

log:
  level: WARNING

circuit_breaker:
  alert_enabled: true
